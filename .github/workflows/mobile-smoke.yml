name: Mobile Smoke Tests

on:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  mobile-web-smoke:
    name: Mobile Web Smoke E2E
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-ci
        with:
          build-domains: 'true'

      - name: Install Playwright browser
        run: npx playwright install --with-deps chromium

      - name: Run mobile viewport smoke tests
        env:
          PLAYWRIGHT_WEB_SERVER_COMMAND: npx vite --host 127.0.0.1 --port 3000 --strictPort
          PLAYWRIGHT_WEB_SERVER_TIMEOUT_MS: '120000'
        run: npm run test:e2e -- --project=db-mobile-e2e
