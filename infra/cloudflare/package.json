{
  "name": "claim-cloudflare-worker",
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev --env development",
    "deploy:dev": "wrangler deploy --env development",
    "deploy": "wrangler deploy --env production",
    "tail": "wrangler tail",
    "login": "wrangler login"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.47.0"
  }
}
