# @ocentra/credentials-domain

Credentials contract domain (types and interfaces only).

## Export

- `SecretAdapter`

## Purpose

Defines the adapter contract for secret retrieval so each runtime can provide its own implementation without coupling secret storage into shared domain logic.


## Scripts

- `npm run build`
- `npm run type-check`
- `npm run lint`
