# @ocentra/auth-domain

Auth bridge contract domain (types and interfaces only).

## Export

- `AuthBridge`

## Purpose

Defines the authentication bridge contract used by consumers to integrate runtime-specific auth providers while keeping shared modules decoupled from any single auth implementation.


## Scripts

- `npm run build`
- `npm run type-check`
- `npm run lint`
