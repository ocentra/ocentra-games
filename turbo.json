{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [".env*", "package-lock.json", "scripts/**"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "inputs": [
        "src/**",
        "scripts/**",
        "public/**",
        "Resources/**",
        "index.html",
        "vite.config.*",
        "tsconfig*.json",
        "package.json"
      ]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^build", "^lint:exec"],
      "outputs": [],
      "inputs": ["src/**", "tests/**", "scripts/**", "tsconfig*.json", "package.json", "eslint.config.*"]
    },
    "lint:exec": {
      "dependsOn": ["^build", "^lint:exec"],
      "outputs": [],
      "inputs": ["src/**", "tests/**", "scripts/**", "tsconfig*.json", "package.json", "eslint.config.*"]
    },
    "type-check": {
      "dependsOn": ["^build"],
      "outputs": [],
      "inputs": ["src/**", "tests/**", "scripts/**", "tsconfig*.json", "package.json"]
    }
  }
}
